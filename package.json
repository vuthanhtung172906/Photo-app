{
  "name": "json-server",
  "version": "1.0.0",
  "description": "Json-server VTT",
  "main": "main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "node gen-data.js && nodemon main.js ",
    "start": "node main.js"
  },
  "keywords": [
    "json-server"
  ],
  "author": "VTT",
  "license": "MIT",
  "dependencies": {
    "faker": "^5.5.3",
    "json-server": "^0.16.3",
    "query-string": "^7.0.1"
  },
  "devDependencies": {
    "nodemon": "^2.0.12"
  }
}
