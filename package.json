{
  "name": "json-server",
  "version": "1.0.0",
  "description": "Json-server VTT",
  "main": "main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "nodemon main.js",
    "start": "node gen-data.js && node main.js"
  },
  "keywords": [
    "json-server"
  ],
  "author": "VTT",
  "license": "MIT",
  "dependencies": {
    "json-server": "^0.16.3"
  },
  "devDependencies": {
    "faker": "^5.5.3",
    "nodemon": "^2.0.12"
  }
}
